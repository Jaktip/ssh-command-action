name: Test action

on:
  push:
    branches: 
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Execute sshpass
        id: execute-sshpass-command
        uses: ./
        with:
          host: ${{ secrets.HOST}}
          username: ${{ secrets.USERNAME}}
          password: ${{ secrets.PASSWORD}}
          command: /home/shared/wsg_backend/check_service_start_up_message.sh ${{ secrets.MODULE}}
      - name: Display Execut sshpass output 
        run: echo "${{ steps.execute-sshpass-command.outputs.command_execution_stdout }}"